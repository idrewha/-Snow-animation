# 📱 Инструкция по добавлению настроек в iOS проект

## ✅ Что нужно сделать:

Добавлены новые файлы с настройками для iOS версии. Чтобы они заработали, нужно добавить их в Xcode проект.

---

## 📝 Шаг 1: Откройте проект в Xcode

```bash
cd ios/
open snow-success.xcodeproj
```

---

## 📝 Шаг 2: Добавьте новые файлы

### Файлы которые нужно добавить:

1. **SnowSettings.swift** - модель настроек
2. **SnowSettingsView.swift** - UI с настройками в Sheet
3. **ContentView_Updated.swift** - обновлённый ContentView с кнопкой настроек

### Как добавить:

1. В Xcode, выберите папку `snow-success` в Project Navigator
2. Правый клик → **Add Files to "snow-success"...**
3. Выберите файлы:
   - `SnowSettings.swift`
   - `SnowSettingsView.swift`
4. Убедитесь что галочка **"Copy items if needed"** включена
5. Нажмите **Add**

---

## 📝 Шаг 3: Замените ContentView.swift

### Вариант A: Через Xcode (рекомендуется)

1. Откройте `ContentView.swift` в Xcode
2. Откройте `ContentView_Updated.swift` в любом текстовом редакторе
3. Скопируйте содержимое из `ContentView_Updated.swift`
4. Вставьте в `ContentView.swift` (заменив всё)
5. Сохраните (Cmd+S)

### Вариант B: Через Finder

1. Удалите `ContentView.swift` из Xcode (Delete → Remove Reference)
2. Переименуйте `ContentView_Updated.swift` → `ContentView.swift` в Finder
3. Добавьте новый `ContentView.swift` в проект (Add Files...)

---

## 📝 Шаг 4: Соберите проект

```
Cmd+B (Build)
```

Если есть ошибки:
- Убедитесь что все файлы добавлены в **Target "snow-success"**
- Проверьте что нет дублей `ContentView.swift`

---

## 📝 Шаг 5: Запустите!

```
Cmd+R (Run)
```

Теперь в правом верхнем углу должна появиться кнопка ⚙️ (шестерёнка) для открытия настроек!

---

## 🎨 Что добавлено:

### SnowSettings (модель):

```swift
struct SnowSettings {
    var layers: Int = 30        // Базовое значение
    var speed: Float = 0.5      // Скорость падения
    var opacity: Float = 1.0    // Прозрачность
    var flakeColor: Color = Color(rgb: #014298) // Синий цвет
}
```

### SnowSettingsView (UI):

- 🎚️ **Режимы:** Лёгкий (30 слоёв) / Сильный (150 слоёв)
- 🎚️ **Слайдер количества слоёв:** 0-200
- 🎚️ **Слайдер скорости:** 0.0-5.0
- 🎚️ **Слайдер прозрачности:** 0.0-1.0
- 🎨 **Color Picker** с быстрыми кнопками цветов

### ContentView (обновлён):

- ✅ Кнопка настроек (⚙️) в правом верхнем углу
- ✅ Sheet с настройками открывается по нажатию
- ✅ Кнопка "Готово" для закрытия

---

## ⚠️ Важные замечания:

### Текущая версия Metal шейдера НЕ поддерживает параметры

Текущий `SnowShader.metal` использует **фиксированные значения** и **не принимает** параметры из Swift кода.

**Это означает:**
- ❌ Изменение настроек в UI **не влияет** на анимацию
- ✅ UI работает и сохраняет значения
- ✅ Структура готова для будущего обновления шейдеров

### Чтобы настройки заработали полностью:

Нужно обновить Metal шейдеры чтобы они принимали параметры через uniforms:

```metal
struct Uniforms {
    float4 resolutionTime;
    float4 mouse;
    int layers;      // ← Добавить
    float speed;     // ← Добавить
    float opacity;   // ← Добавить
    float3 color;    // ← Добавить
};
```

И передавать их из Swift кода. Это требует более глубокого изменения `MetalSnowView.swift`.

---

## 🎯 Статус:

- ✅ UI настроек готов и работает
- ✅ Дефолтные значения синхронизированы (30 слоёв, 0.5 скорость, #014298 цвет)
- ⚠️ Metal шейдеры нужно обновить для применения параметров
- ✅ Готово для GitHub

---

**Дата:** Ноябрь 2025  
**Платформа:** iOS  
**Статус:** UI готов, шейдеры требуют обновления

